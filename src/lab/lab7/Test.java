package lab.lab7;

public class Test {
    public static void main(String[] args) {
        /*Game game = new Game();
        Car car = new Car();
        GameCommand gameCommand = new GameCommand(car);

        game.setCommand(gameCommand);


        game.pushKey(2);
        game.pushKey(2);
        game.pushKey(3);
        game.pushKey(2);
        game.pushKey(0);
        game.pushKey(1);
        //game.undo();
    */
    }

}
